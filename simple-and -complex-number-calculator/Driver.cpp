#include<iostream>
#include"ComplexNumber.h"
#include"SimpleNumber.h"
using namespace std;
int main()
{

/*
	int choice;
	calculator *p=nullptr;
	bool back = false;
	do
	{


			cout << " press 1 for simple number " << endl;
			cout << " press 2 for complex number " << endl;
			cin >> choice;
			if (choice == 1)
			{
				p = new SimpleNumber;
			}
			else if (choice == 2)
			{
				p = new ComplexNumber;
			}
			else
			{
				back = true;
			}
		
	} while (back);
	p->setA();
	p->setB();
	p->add();
	p->subtract();
	p->multiply();
	p->divide();

	delete p;*/
	system("pause");
	return 0;
}





